#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'Dijkstra test for any versions.',
        'data' => [ ],
        'tests' => [qw(
            doc-mgDijkstra
            )],
        'documentation' => [qw(
            doc-mgDijkstra
            )]
    },

);

1;
