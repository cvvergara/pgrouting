#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'Dijkstra test for any versions.',
        'data' => [ ],
        'tests' => [qw(
            doc-ankurDijkstra
            )],
        'documentation' => [qw(
            doc-ankurDijkstra
            )]
    },

);

1;
