#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'Dijkstra test for any versions.',
        'data' => [ ],
        'tests' => [qw(
            doc-vidhanDijkstra
            )],
        'documentation' => [qw(
            doc-vidhanDijkstra
            )]
    },

);

1;
