#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => '',
        'data' => [],
        'tests' => [qw(
            withPoints-category
            sampledata
            migration
            concepts
            )],
        'files' => [qw(
            sampledata
            withPoints-category
            migration
            concepts
            )],
    },
);

1;
