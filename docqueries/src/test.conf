#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'files' => [qw(
            sampledata
            withPoints-category
            migration
            concepts
            )],
    },
);

1;
