#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'Ordering algorithm tests.',
        'data' => [ ],
        'tests' => [qw(
            cuthillMckeeOrdering
            )],
        'files' => [qw(
            cuthillMckeeOrdering
            )]
    },

);

1;