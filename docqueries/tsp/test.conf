#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'TSP tests',
        'data' => [],
        'tests' => [qw(
            doc-pgr_TSPeuclidean
            doc-pgr_TSP
            )],
        'documentation' => [qw(
            doc-pgr_TSPeuclidean
            doc-pgr_TSP
            )]
    },
);

1;
