#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'files' => [qw(
            lineGraphFull.pg
            lineGraph.pg
            pgtap_design_lineGraphFull
            proofOfConcept1
            proofOfConcept2
            )],
    },
);

1;
