#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'topologicalSort algorithm tests.',
        'data' => [ ],
        'tests' => [qw(
            doc-topologicalSort
            )],
        'files' => [qw(
            doc-topologicalSort
            )]
    },

);

1;
