#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'pgr_hawickCircuits algorithm tests',
        'data' => ["" ],
        'tests' => [qw(
            hawickCircuits
            )],
        'files' => [qw(
            hawickCircuits
            )]
    },
);

1;
