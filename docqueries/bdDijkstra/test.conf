#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'data' => ["large.data" ],
        'files' => [qw(
            bdDijkstra-large
            bdDijkstra.pg
            bdDijkstraCost.pg
            bdDijkstraCostMatrix.pg
            )]
    },
);

1;
