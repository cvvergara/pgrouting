#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'Dijkstra test for any versions.',
        'data' => [ ],
        'tests' => [qw(
            doc-pgr_bellmanFord
            doc-pgr_edwardMoore
            )],
        'files' => [qw(
            doc-pgr_bellmanFord
            doc-pgr_edwardMoore
            )]
    },

);

1;
